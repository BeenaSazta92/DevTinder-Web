## DevTinder-Web

-Create a vite + react application
-Remove unnessary code and create a Hello World app
-manin.js ========
Main entry point of your application where you create the root of this application

-Install tailwind css
-install diasy ui tailwind css plugin 
-create seprate navbar component 
-Install react router dom (routing done at the root/top of application)
-

create browserRouter ? Routes =/ Body > BodyChildren
-Create an Outlet in your body component
-Create Footer
-create browser router > routes >Route ? RouteChildren
-Create Outlet in body components
-Create a login page
-Install axios
-CORS- install cors in backend => add middleware to with configuration : origin, credentials :true
- Whenever you are making API call to pass axios =>{withcredentials:true}
-Install react-redux + @redux/toolkit 
-ConfigureStore => Provider =>CreateSlice => add reducer to store
-NavBae should update as soon as user logged in 
Refactor code and creae constant file
-Create component folder and add move all component files inside component folder to restructure 
-Restrict user to access other routes without login
-If token is not present redirect to login
-Logout Functionality
-Profile




redux tookkit extension






Body

    NavBar
    Route =/ =>Feed
    Route =/login => login
    Route =/connections =>Connections
    Route =/profile => Profiles







